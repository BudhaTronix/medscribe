apiVersion: v1
kind: Service
metadata:
  name: clinical-voice-note-api
  namespace: clinical-voice-note
spec:
  type: ClusterIP
  selector:
    app: clinical-voice-note-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
